Delegates to the `external-sync-consistency` skill (`.claude/skills/external-sync-consistency/SKILL.md`).

Run the AI Project OS external sync consistency validator. Compare repo source records, local sync map, committed logs, and (optionally) live read-only external state for Google Calendar and GitHub Projects.

Always read-only. Never mutates external systems.

Follow the full protocol in `.claude/skills/external-sync-consistency/SKILL.md`.
