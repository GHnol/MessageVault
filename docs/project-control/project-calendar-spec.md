# Project Calendar / Schedule Spec (SUPERSEDED)

**Status:** SUPERSEDED by `calendar-spec.md` (Package 2.8).
**Superseded:** 2026-05-17.
**Kept for history** — the old "NOT BUILT / gitignored, never committed" language below is no longer current.

---

## What changed

Package 2.7 created this as a spec-only stub that assumed the calendar would be generated and gitignored. Package 2.8 built the calendar as a **repo-native, committed** artifact (explicit Coordinator instruction). The authoritative calendar spec is now **`calendar-spec.md`**, and the importable file is **`keepmees-project-calendar.ics`** (a surgical `.gitignore` exception was added so it is trackable).

## Use instead

- Calendar rules and ritual definitions → `calendar-spec.md`
- Importable Google Calendar file → `keepmees-project-calendar.ics`
- Import instructions → `calendar-spec.md` ("How to import into Google Calendar")

## Preserved principle

The calendar is a view of the schedule, which is a view of the roadmap. It holds review rituals and milestone/gate checkpoints only — never per-task noise, never unique truth. That principle (from Package 2.7) is carried forward into `calendar-spec.md`.
